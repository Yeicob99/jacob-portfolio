---
const PROJECTS = [
  {
    title: "Sirius Star Lights – Custom Car Lighting",
    description:
      "An e-commerce site I built for a car lighting brand. The goal was to create a bold and modern look that matches the unique style of their products.",
    link: "https://siriusstarlights.com",
    img: "./images/SiriusStarLights-img.gif",
  }
];
---

{PROJECTS.map(({ img, title, description, link }) => (
  <article class="space-y-6">
    <h3 class="text-lg font-semibold text-yellow-100/90">{title}</h3>
    <p class="text-lg text-pretty">{description}</p>
    <img class="rounded shadow-2xl shadow-white/15" src={img} alt={title} />
    {link && (
      <p>
        <a
          href={link}
          target="_blank"
          rel="noopener noreferrer"
          class="inline-flex items-center px-4 py-2 text-sm rounded-lg
                 bg-white/5 border border-white/10 text-slate-100
                 hover:bg-white/10 transition"
        >
          View Website
          <svg class="w-3 h-3 ms-2 rtl:rotate-180" aria-hidden="true" xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 14 10">
            <path stroke="currentColor" stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M1 5h12m0 0L9 1m4 4L9 9"/>
          </svg>
        </a>
      </p>
    )}
  </article>
))}
